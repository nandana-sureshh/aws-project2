apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: {{ .Values.namespace }}
  labels:
    app: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
  - port: 80
    targetPort: {{ .Values.services.appointmentService.port }}
  type: ClusterIP
