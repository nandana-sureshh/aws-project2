apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: document-service-route
  namespace: {{ .Values.namespace }}
spec:
  parentRefs:
  - name: caresync-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /api/document
    backendRefs:
    - name: document-service
      port: 80
