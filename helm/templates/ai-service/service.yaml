apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: {{ .Values.namespace }}
  labels:
    app: ai-service
spec:
  selector:
    app: ai-service
  ports:
  - port: 80
    targetPort: {{ .Values.services.aiService.port }}
  type: ClusterIP
