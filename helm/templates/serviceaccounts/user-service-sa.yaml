apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service-sa
  namespace: {{ .Values.namespace }}
  labels:
    app: user-service
    {{- include "caresync.labels" . | nindent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::${AWS_ACCOUNT_ID}:role/caresync-dev-user-service-role"
automountServiceAccountToken: true
